# Support channel mapping in setup

The Quest-Native Flight Station should model pilot intent in the cockpit, but it must support Channel Mapping Setup so the pilot can match Quest controls to the RC channels expected by the drone's Betaflight configuration. This keeps the app outside the Betaflight configurator role while still handling the real first-time setup needed for ExpressLRS micro FPV drones.
